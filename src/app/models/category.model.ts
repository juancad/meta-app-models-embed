export class Category {
    name: string;
    minVal: number;
    maxVal: number;

    constructor(name: string, minVal: number, maxVal: number) {
        this.name = name;
        this.minVal = minVal;
        this.maxVal = maxVal;
    }
}
